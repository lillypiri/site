<%= render 'section', title: 'News', photos: @photos.tagged('lillypirinews') %>
<%= render 'section', title: 'Daily Painting Project', photos: @photos.tagged('lillypiripaints') %>
<%= render 'section', title: 'Still Life', photos: @photos.tagged('lillypiristilllife') %>

<h1>Shop</h1>
<% @items.all.each do |item| %>
  <%= item.title %>
  <%= link_to image_tag(item.image_url, class: 'img-responsive'), item.external_url %>
<% end %>
