<%= render 'section', title: 'News', photos: @photos, tag: "lillypirinews" %>
<%= render 'section', title: 'Daily Painting Projects', photos: @photos, tag: "lillypiripaints" %>
<%= render 'section', title: 'Still Life', photos: @photos, tag: "lillypiristilllife" %>

<h2><a href="https://lillypiri.etsy.com">Shop</a></h2>
<div class="grid">
  <% @items.each do |item| %>
    <div class="cell">
      <%= link_to image_tag(item.image_url, class: 'img-responsive'), item.external_url %>
    </div>
  <% end %>
</div>
